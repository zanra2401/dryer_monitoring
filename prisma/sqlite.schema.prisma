generator client {
  provider = "prisma-client"
  output   = "../app/generated/sqlite"
}

enum FlagType {
    NUMBER
    STRING
    BOOLEAN
}

datasource db {
  provider = "sqlite"
}

model SystemFlag {
  key   String @id
  value String @unique
  type  FlagType
}
